$('.flash').html('<%= j render "shared/flash_errors"%>');

$('[name="answer[body]"]').val('');

$('.edit-answer-link').click(function(e){
    e.preventDefault();
    $(this).hide();
    const answer_id = $(this).data('answerId');
    $('form#edit-answer-' + answer_id).show();
});